<form method="post" action="{{ path('app_vaccin_injection_delete', {'id': vaccin_injection.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vaccin_injection.id) }}">
    <button class="btn">Delete</button>
</form>
